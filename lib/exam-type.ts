// Mirrors the public.exam_type enum (0085) -- which cohort a student
// belongs to. Drives which curriculum, subject pickers and general-exam
// shape the (otherwise shared) task UI shows.
export type ExamType = "YKS" | "LGS";
